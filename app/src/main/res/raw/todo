//TODO 1. Трабл со строками, когда при ComponentBuilder создаются java strings, а не kotlin. Исправил костыльно через if в ElementScanner8.
//TODO 2. В котлине конструктор создает сразу и поля и параметры. Если в предке компонента поля конструктора без "open val", то будет ошибка "override". Нужно различать голые поля от параметров.